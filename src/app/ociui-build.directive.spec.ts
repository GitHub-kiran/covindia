import { OciuiBuildDirective } from './ociui-build.directive';

describe('OciuiBuildDirective', () => {
  it('should create an instance', () => {
    const directive = new OciuiBuildDirective();
    expect(directive).toBeTruthy();
  });
});
