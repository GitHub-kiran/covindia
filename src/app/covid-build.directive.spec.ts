import { CovidBuildDirective } from './covid-build.directive';

describe('CovidBuildDirective', () => {
  it('should create an instance', () => {
    // const directive = new CovidBuildDirective();
    // expect(directive).toBeTruthy();
  });
});
